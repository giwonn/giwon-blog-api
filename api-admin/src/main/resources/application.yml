server:
  port: 8081

spring:
  application:
    name: giwon-blog-admin-api
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  datasource:
    url: jdbc:postgresql://localhost:5432/giwon_blog
    username: giwon
    password: giwon1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

image:
  storage:
    path: ${IMAGE_STORAGE_PATH:/data/blog/images}
    public-url: ${IMAGE_PUBLIC_URL:http://localhost:8080/images}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3001}
